services:
  grsai-proxy:
    build: .
    ports:
      - "1515:1515"
    volumes:
      - ./.env:/app/.env
      - ./data:/app/data
      - ./results:/app/results
    restart: unless-stopped

# 使用前请确保以下文件/目录存在于项目根目录：
# - .env (已包含在仓库中)
# - data/ (首次运行自动创建 keys_cache.json 与 .password)
