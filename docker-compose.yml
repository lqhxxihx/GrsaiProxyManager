services:
  grsai-proxy:
    build: .
    ports:
      - "1515:1515"
    volumes:
      - ./.env:/app/.env
      - ./keys_cache.json:/app/keys_cache.json
      - ./.password:/app/.password
    restart: unless-stopped
